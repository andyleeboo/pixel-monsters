{
	"name": "pixel-monsters",
	"version": "1.0.0",
	"private": true,
	"description": "Deterministic pixel monsters — seed in, animated SVG out. Zero dependencies.",
	"type": "module",
	"scripts": {
		"test": "node --test",
		"generate": "node generate.mjs"
	},
	"engines": {
		"node": ">=18"
	}
}
